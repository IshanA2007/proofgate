# Copy to app/.env for local development. Production: set as real env vars.
APP_ID=
# Either inline the PEM (escape newlines as \n) or use PRIVATE_KEY_PATH.
PRIVATE_KEY=
# PRIVATE_KEY_PATH=/path/to/proofgate.private-key.pem
WEBHOOK_SECRET=
PORT=3000
# Local development only — created automatically by the setup wizard:
# WEBHOOK_PROXY_URL=https://smee.io/your-channel
